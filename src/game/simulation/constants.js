import { tileAssets } from '../assets/manifest.js'

export const GRID_SIZE = 8
export const TARGET_SCORE = 3200
export const MOVE_LIMIT = 26
export const SCORE_PER_TILE = 50
export const TILE_TYPES = tileAssets.map((tile) => tile.key)

